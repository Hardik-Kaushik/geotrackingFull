<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
    <script>
        function capitalizeFirstLetter(string) {
            return string.charAt(0).toUpperCase() + string.slice(1);
        }

        document.addEventListener("DOMContentLoaded", function() {
            var username = "<%= user.username %>";
            document.getElementById('username').innerText = capitalizeFirstLetter(username);
        });
    </script>
    <style>
        /* General Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-image: url(/Map-UI.png.webp);
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            color: #333;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }

        .content {
            background: rgba(240, 171, 248, 0.899);
            padding: 20px;
            border-radius: 30px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            width: 90%;
            height: 70%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        h1 {
            font-size: 3.5em;
            margin-bottom: 20px;
        }

        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #6200ea;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #3700b3;
        }

        form {
            margin-top: 20px;
        }

        form button {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            background-color: #6200ea;
            color: #fff;
            transition: background-color 0.3s ease;
        }

        form button:hover {
            background-color: #3700b3;
        }

        /* Media Queries */
        @media (max-width: 768px) {
            .content {
                padding: 15px;
            }

            h1 {
                font-size: 1.5em;
            }

            .btn, form button {
                font-size: 14px;
                padding: 8px 15px;
            }
        }

        @media (max-width: 480px) {
            .content {
                padding: 10px;
            }

            h1 {
                font-size: 1.2em;
            }

            .btn, form button {
                font-size: 12px;
                padding: 6px 10px;
            }
        }
    </style>
</head>
<body>
    <div class="content">
        <h1>Welcome, <span id="username"></span>!</h1>
        <% if (user.role === 'admin') { %>
            <a href="/admin" class="btn">Go to Admin Dashboard</a>
        <% } else { %>
            <form id="geotrackingForm" action="/geotracking" method="GET">
                <button type="submit" class="btn">Start Geotracking</button>
            </form>
            <br><br>
            <a href="/internal-navigation" class="btn">Internal Navigation</a>
        <% } %>
        <!-- Add Internal Navigation button -->
        <br><br>
        
    </div>
</body>
</html>
